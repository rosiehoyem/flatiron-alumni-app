<section id="content">
  <div class="container"> 
    <div class="row-fluid">
        <h1>Edit Job Posting</h1>
        <br>
        <% if !current_user || current_user.employer == false %>
          <br>
          You Don't have permission to Post a Job:(
            <% elsif current_user.id == @job.user_id.to_i %> 
          <div class="span6">
             <%= render 'form' %>
          </div>
          <div class="span5 pull-right"><br><br>
            <%= image_tag("lapt.png", :style => "width: 80%; border-radius: 5px;") %>
          </div>
          
        <% end %>
      </div>
      
</div>

</section>

<%= render 'shared/bottom_section' %>
