<section id="content">
  <div class="container"> 
    <div class="row-fluid">
      <div class="span12">

      <% if !current_user || current_user.employer == false %>
      <br>
      Only Employers May Post Jobs :(

      <% elsif current_user.id == @job.user_id.to_i %> 
      
      <%= render 'form' %>

      <% end %>
        
      </div>
    </div>
  </div>
</section>

<%= render 'shared/bottom_section' %>




